name: DARKI Link Android build

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: read

env:
  ANDROID_HOME: /usr/local/lib/android/sdk
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK packages
        run: |
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses < /dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            "platform-tools" \
            "platforms;android-35" \
            "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build DARKI Link debug APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Build DARKI Link release bundle
        run: gradle --no-daemon :app:bundleRelease

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: darki-link-debug-apk
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload release bundle
        uses: actions/upload-artifact@v4
        with:
          name: darki-link-release-bundle
          path: app/build/outputs/bundle/release/*.aab
